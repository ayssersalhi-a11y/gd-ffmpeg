# سجل تغييرات ffmpeg_player.h / ffmpeg_player.cpp

GDExtension — FFmpeg Video Player (Unified) for Godot 4 (Android ARM64/ARM32)

هذا الملف يوثّق كل إصلاح وسبب وجوده بالتفصيل، بدل تعبئة رأس ملفي العمل
(`.h`/`.cpp`) بنصوص تاريخية طويلة لا تفيد قراءة الكود اليومية. راجع هذا
الملف عند الحاجة لفهم "لماذا" وراء أي سطر كود يحمل وسمًا مثل `[FIX-x]` أو
`[اسم الميزة v#.#]`.

---

## v7.10 — خطوة 4 (نسخة آمنة) من مقارنة EIRTeam.FFmpeg: حالة تشغيل صريحة

- **[STATE-MACHINE]** آخر خطوة من قائمة مقارنة EIRTeam.FFmpeg الأصلية،
  لكن بتصميم أكثر أمانًا عمدًا. EIRTeam يستخدم `enum DecoderState` حقيقيًا
  يستبدل الأعلام الداخلية بالكامل. **قررنا عدم فعل هذا** — استبدال
  `playing`/`buffering` الداخليين يعني إعادة كتابة عشرات الشروط في
  `_process()`/`play()`/`pause()`/`stop()`/`seek()`، وهو بالضبط المنطق
  الذي أُصلِح واستقر عبر هذه المحادثة الطويلة (v6.4، v7.1، v7.2...).
  خطر انحدار حقيقي على نظام مُختبَر جيدًا مقابل فائدة تنظيمية بحتة.

  بدلًا من ذلك: `get_playback_state()` — دالة حساب **للقراءة فقط**، تُشتق
  من الأعلام الداخلية الموجودة فعليًا (`fmt_ctx`, `decoder_faulted`,
  `buffering`, `playing`, `explicitly_stopped`) دون تغييرها أو التأثير
  عليها إطلاقًا. تُعيد عددًا صحيحًا يقابل:
  `0=NONE, 1=STOPPED, 2=PAUSED, 3=BUFFERING, 4=PLAYING, 5=FAULTED`.

  عضوان جديدان يدعمان هذا: `explicitly_stopped` (يُميّز STOPPED بعد
  `stop()` عن PAUSED بعد `pause()`)، و`decoder_faulted` (atomic — حالة
  فشل فك تشفير غير قابلة للتعافي، عتاد وبرمجي معًا فشلا). هذا كشف أيضًا
  **فجوة حقيقية كانت موجودة منذ v7.9**: فشل `_try_disable_hw_decoding()`
  الكامل كان يُسكِت الفيديو بصمت بلا أي إشارة خطأ. أُصلحت الآن عبر نفس
  نمط الأعلام الذرية المُهدَّأة المستخدم أصلًا لأخطاء الشبكة (`decoder_
  fault_notified` يضمن إصدار `playback_error` مرة واحدة فقط، بأمان من
  الخيط الرئيسي حصرًا — استدعاء `emit_signal` مباشرة من خيط الفك كان
  سيكون غير آمن).

  **بهذا اكتملت أربع خطوات من مقارنة EIRTeam.FFmpeg** (طابور أوامر،
  تجميع ذاكرة، تراجع عتاد، حالة صريحة) — الخطوتان المتبقيتان من القائمة
  الأصلية (تجريد عابر للمنصات لاختيار العتاد، أغلفة RAII لموارد FFmpeg)
  لم تُنفَّذا؛ الأولى ذات فائدة محدودة لمشروع أندرويد فقط، والثانية
  إعادة هيكلة واسعة لا تستحق الخطر دون سبب ملحّ حاليًا.

## v7.9 — خطوة 3 من مقارنة EIRTeam.FFmpeg: تراجع تلقائي من العتاد للبرمجي

- **[HW-FALLBACK]** نمط مقتبس من `_try_disable_hw_decoding()` في
  EIRTeam.FFmpeg. سابقًا: نختار ديكودر العتاد (MediaCodec) مرة واحدة فقط
  عند الفتح، وإن فشل لاحقًا **أثناء** التشغيل الفعلي (خطأ متكرر من
  `avcodec_send_packet`/`avcodec_receive_frame` غير EAGAIN)، لا نملك أي
  شبكة أمان — الفيديو يتوقف بصمت. كذلك إن فشل فتح ديكودر العتاد من
  البداية أصلاً، كنا نستسلم كليًا بدل تجربة البرمجي.

  الآن: `_setup_video_codec()` يُجرِّب البرمجي تلقائيًا إن فشل فتح العتاد
  عند التحميل الأولي. أثناء التشغيل، `_handle_decode_error()` يُستدعى من
  كلا مساري الفك (خيط الفك الشبكي، ومسار الفك المتزامن للملفات المحلية)
  عند أي خطأ حقيقي (ليس EAGAIN)، ويزيد عدّاد فشل متتالٍ (`hw_decode_fail_count`)
  يُصفَّر تلقائيًا عند أي نجاح. بعد `HW_DECODE_FAIL_THRESHOLD` (10) فشلًا
  متتاليًا، `_try_disable_hw_decoding()` يُغلق جلسة MediaCodec ويفتح
  ديكودر برمجي بديل بنفس معاملات التيار، محافظًا على استمرارية التشغيل
  (قد يحدث تقطّع بصري بسيط لحظة التبديل حتى وصول أول إطار مفتاحي جديد —
  متوقَّع وأفضل من توقف كامل). `sws_ctx` يُعاد إنشاؤه أيضًا بعد التبديل
  (تنسيق البكسل قد يختلف بين مخرجات العتاد والبرمجي).

## v7.8 — خطوة 2 من مقارنة EIRTeam.FFmpeg: تجميع إعادة استخدام الذاكرة (Object Pooling)

- **[OBJECT-POOL]** نمط مقتبس من `available_textures`/`hw_transfer_frames`
  في EIRTeam.FFmpeg. سابقًا: تخصيص `PackedByteArray` جديد كامل الحجم
  (~1-2 ميجابايت للفيديو النموذجي) في **كل إطار مفكوك**، وكائن `Image`
  جديد في **كل إطار معروض** (حتى 60 مرة/ثانية أثناء التشغيل) — ضغط
  تخصيص/تحرير متكرر غير ضروري على الذاكرة، خصوصًا على أجهزة محدودة.

  أُضيف مجمّع مخازن بايت (`free_frame_buffers` + `_acquire_frame_buffer()`
  + `_release_frame_buffer()`، محمي بقفل `frame_pool_mutex` منفصل، سقف
  `MAX_POOLED_BUFFERS=12`) — يُستخدم في كلا مساري الفك (خيط الفك الشبكي
  ومسار الفك المتزامن للملفات المحلية). كل نقطة تُسقِط/تمسح إطارًا
  (Hard Frame Drop، Fast Seek، أمر التفريغ عند Seek شبكي، `_clear_queues`،
  حلقة الإسقاط في `_present_frame_at`) تُعيد المخزن للمجمّع بدل تركه يُحرَّر.

  كائن `Image` واحد (`reusable_present_image`) يُعاد استخدامه عبر
  `set_data()` بدل `Image::create_from_data()` الذي كان يُنشئ كائنًا
  جديدًا في كل إطار معروض.

  **أمان مضمون عبر Copy-on-Write:** حتى لو كان مخزن مُعاد للمجمّع لا يزال
  مُشارَكًا مع كائن آخر (مثل نسخة `Image` الحالية المعروضة)، فإن الكتابة
  اللاحقة عليه عبر `PackedByteArray::ptrw()` تُفعِّل آلية Copy-on-Write في
  Godot تلقائيًا (تُنشئ نسخة خاصة قبل الكتابة) — فلا خطر تلف بيانات، أسوأ
  حالة هي فقط عدم تحقيق توفير في تلك الحالة المحدَّدة (لا فساد بيانات أبدًا).

  المجمّع وكائن الصورة يُصفَّران في `_cleanup()` عند تحميل فيديو جديد
  (أبعاد مختلفة تُبطل فائدة المخازن القديمة).

## v7.7 — خطوة 1 من مقارنة EIRTeam.FFmpeg: طابور أوامر عام لخيط الفك

- **[COMMAND-QUEUE]** أول خطوة من سلسلة تحسينات مستوحاة من مقارنة كودنا
  بمشروع `EIRTeam.FFmpeg` الناضج (315+ نجمة، مُستخدَم في لعبة تجارية).
  استبدلنا `decode_flush_requested`/`decode_flush_done` (زوج أعلام Atomic
  خاص بحالة واحدة فقط) بطابور أوامر عام (`decode_command_queue` +
  `_post_decode_command()`) — نمط مبني على فكرة `CommandQueueMT` في محرك
  Godot نفسه (والتي يستخدمها EIRTeam.FFmpeg لنفس الغرض بالضبط، عبر إعادة
  تطبيق خفيفة خاصة بهم لأن الصنف الأصلي داخلي وغير متاح عبر godot-cpp).
  الآن أي ميزة مستقبلية تحتاج تنفيذ عمل على خيط الفك (وليس فقط التفريغ عند
  Seek) تستدعي `_post_decode_command()` بإغلاق (lambda) مباشرة، دون
  الحاجة لاختراع زوج أعلام Atomic جديد لكل حالة كما كنا نفعل سابقًا.
  إشارة "اكتمل" (`decode_flush_done`) بقيت كما هي — خاصة بهذا الأمر
  تحديدًا، فالطابور مسؤول فقط عن إيصال العمل، لا عن الإبلاغ بإنجازه.

  **الخطوات التالية المخطَّطة من نفس المقارنة (لم تُنفَّذ بعد):** تجميع
  إعادة استخدام الكائنات (Object Pooling) للنسيج والإطارات، التراجع
  التلقائي من العتاد للبرمجيات عند فشل MediaCodec أثناء التشغيل، آلة حالة
  صريحة بدل أعلام Boolean متناثرة، تجريد عابر للمنصات لاختيار العتاد،
  أغلفة RAII لموارد FFmpeg.

## v7.6 — خيط فك تشفير مستقل (للفيديو الشبكي فقط)

- **[DECODE-THREAD]** إضافة إلى الحل الجشع (v7.5.1): خيط `_decode_thread_worker`
  مستقل تمامًا، يبدأ فقط عندما `video_is_network_source == true` (الملفات
  المحلية تبقى على المسار المتزامن القديم بلا أي تغيير — لم تُظهر المشكلة
  أصلًا). هذا الخيط هو المالك الحصري لـ `video_codec_ctx`/`sws_ctx` بمجرد
  بدئه؛ يسحب حزمًا من `video_packet_queue` (نفس طابور `_network_read_worker`)
  ويُطعمها لـ MediaCodec باستمرار دون انتظار دوره ضمن معدل إطارات
  `_process()` — يحل هذا مشكلة "عمق أنبوب MediaCodec" (بعض الأجهزة تحتاج
  ~100-140 حزمة قبل إخراج أول إطار) بشكل مباشر بدل الاعتماد فقط على رفع
  حد الإطعام الجشع لكل Tick (v7.5.1) الذي يبقى محدودًا بمعدل استدعاء
  `_process()` نفسه.

  عمليات `avcodec_flush_buffers`/إعادة إنشاء `sws_ctx` عند Seek على مصدر
  شبكي أصبحت طلبات ذرية (`decode_flush_requested`/`decode_flush_done`)
  يعالجها الخيط بنفسه — بنفس نمط طلبات Seek الشبكية الموجود أصلًا منذ v7.0.
  `decoded_frame_queue` أصبح محميًا بقفل مخصص (`decoded_frame_mutex`)
  منفصل تمامًا عن `network_queue_mutex` (لا تداخل بين القفلين أبدًا، فلا
  خطر ترتيب أقفال/Deadlock). `position` (متغيّر double عادي غير آمن
  للقراءة/الكتابة عبر الخيوط) لا يُقرأ مباشرة من خيط الفك؛ بدلاً منه نسخة
  ذرية مخصصة (`decode_position_hint`) يُحدِّثها الخيط الرئيسي باستمرار.

## v7.5.1 — إصلاح تسخين MediaCodec (JNI) + تشخيص التذبذب الغامض + إطعام جشع أثناء التخزين

- **[GREEDY-DECODE]** السجلات التشخيصية (`[BUF-DIAG]`/`[NET-DIAG]`) كشفت أن
  `_decode_packets_into_queue()` كانت مقيَّدة بحد 50 حزمة كحد أقصى لكل
  استدعاء — حتى أثناء `buffering=true` حيث لا صورة معروضة بعد لنحميها من
  تجمّد لحظي. تبيّن أن بعض أجهزة MediaCodec (Mali-G57 مثلاً) تحتاج تلقيمها
  بحوالي 100-140 حزمة قبل إخراج أول إطار فعليًا — تقييد الإطعام بلا داعٍ
  أثناء الانتظار الأولي كان يُبطئ الوصول لهذا العمق. أصبح للدالة معامل
  `greedy` (افتراضيًا false): عند `true` (يُستخدم فقط أثناء `buffering`)
  يرتفع الحد الأقصى إلى 500 لكل استدعاء بدل 50 — الحلقة نفسها تتوقف تلقائيًا
  فور فراغ الطابور فعليًا (الرقم سقف أقصى آمن، وليس عددًا يُنفَّذ دائمًا).
  أثناء التشغيل الفعلي (بعد بدء العرض) يبقى الحد المحافظ (50) كما كان
  لحماية سلاسة الإطار الحالي من تجمّد لحظي على الخيط الرئيسي.

- **[WARMUP-JNI-ATTEMPT-FAILED]** محاولة نقل التسخين للخيط الرئيسي (بدل
  خيط منفصل) **لم تحل** خطأ `Operation not permitted` — فشل بنفس الخطأ
  رغم كونه على خيط مرتبط بـ JNI يقينًا (الفيديو الحقيقي ينجح على نفس
  الخيط لاحقًا). هذا يُسقط تشخيص "الخيط غير المرتبط بـ JNI" الذي افتُرض في
  v7.5.1 الأولي. **السبب الأرجح الفعلي:** الجلسة الوهمية لا تحتوي على
  `extradata` (بيانات SPS/PPS) صالحة، وMediaCodec يرفض `avcodec_open2()`
  بدونها. لم يُصلَح بعد — يحتاج تضمين بايتات H.264 حقيقية (مخاطرة توافق
  غير مؤكَّدة لم نتخذ قرارًا بشأنها بعد).

- **[BUFFER-OSCILLATION-DIAG]** أُضيفت طباعات `[BUF-DIAG]` (من الخيط
  الرئيسي: حجم كل طابور، مصدر حساب forward_buffer_secs، حالة EOF) و
  `[NET-DIAG]` (من خيط الشبكة: معدّل وصول حزم الفيديو الفعلي) — كشفت أن
  "التذبذب الغامض" في `forward_buffer_secs` (يظهر 0.0 ثم يقفز لأرقام
  كبيرة) طبيعي تمامًا وغير ضار: ناتج عن استهلاك سريع مشروع لطابور الحزم
  (تسليمها للمُفكِّك) أسرع من تجدّد الشبكة له لحظيًا، وليس فقدان بيانات أو
  خطأ منطقي.

## v7.5 — تسخين مُفكِّك MediaCodec مسبقًا (محاولة أولى، بخيط خلفي)

- **[DECODER-WARMUP]** السجلات أظهرت فجوة ثابتة تقريبًا (~2.2-2.7 ثانية)
  بين "الكودك جاهز" و"أول إطار مفكوك" — ثابتة بغض النظر عن سرعة الشبكة
  (لوحظت مع اتصال بطيء وسريع على حدٍّ سواء)، مما رجّح أنها تكلفة إنشاء
  جلسة MediaCodec الحقيقية مع سائق الجهاز، لا انتظار بيانات. فُتحت جلسة
  `h264_mediacodec` وهمية (1280×720، بلا بيانات فعلية) في خيط خلفي منفصل
  عند `_ready()` — مرة واحدة فقط لعمر التطبيق. **(تبيّن لاحقًا فشلها دائمًا
  بخطأ JNI — أُصلح في v7.5.1 أعلاه.)** كما تبيّن من تجارب لاحقة أن الفجوة
  نفسها متذبذبة جدًا (450ms إلى 12+ ثانية) وليست بالضرورة تكلفة MediaCodec
  الثابتة كما افتُرض ابتداءً — لا يزال التشخيص مفتوحًا.

## v7.4 — دعم ترويسة Referer لتفادي تأخير/حجب مضيفي الفيديو المحميين

- **[REFERER]** `load_video(path, referer="")` — معامل اختياري جديد. بعض
  مضيفي الفيديو (Streamtape وأمثاله) يطبّقون حماية مضادة للاستخدام
  المباشر (Anti-Hotlinking) تتحقق من ترويسة Referer، وقد تتعمّد تأخير
  الاستجابة (لا رفضها صراحة) إن غابت — إجراء شائع مضاد للبوتات. مرِّر
  صفحة الفيديو الأصلية كـ Referer عند الحاجة لتفادي هذا التأخير.

## v7.3 — قياس زمني تشخيصي (Timing Diagnostics)

- **[TIMING-DIAG]** لتحديد بدقة أين تُصرَف ثواني التأخير الملاحَظة على
  بعض الروابط (مثل خدمات تتضمن حل رابط عبر توكن مثل Streamtape)، أُضيفت
  طباعات زمنية نسبية لحظة استدعاء `load_video()` عند: نجاح فتح الاتصال،
  نجاح تحليل الصيغة، فك أول إطار فيديو، ولحظة الانطلاق الفعلية (صوت+صورة
  معًا). لا تُغيّر أي سلوك — تشخيص فقط.

## v7.2 — انطلاق سريع حقيقي (تقليل التخزين الإلزامي من 5s إلى 1s)

- **[STARTUP-FIX]** `INITIAL_PLAY` كان 5.0 ثانية — انتظار إلزامي طويل قبل
  أي انطلاق، بصرف النظر عن جودة الشبكة الفعلية. قرار تصميم صارم من v6.4،
  وليس محدودية جهاز أو فك تشفير كما افتُرض سابقًا. التطبيقات الاحترافية
  (يوتيوب، تيك توك) تنطلق بجزء من ثانية إلى ثانية واحدة فقط، معتمدة على
  إعادة التخزين التفاعلية عند الحاجة بدل انتظار مسبق ضخم يتحمّله الجميع
  دائمًا. أصبح 1.0 ثانية فقط، مع بقاء `REBUFFER_TARGET` (2s) لإعادة
  التخزين بعد نضوب فعلي أثناء التشغيل (هامش أكبر عن قصد، لأن النضوب دليل
  شبكة متعثرة بالفعل).

## v7.1 — لا صوت بلا صورة (انتظار أول إطار فيديو جاهز قبل الانطلاق)

- **[DECODE-GATE]** كان الخروج من `buffering` يعتمد فقط على توفر بيانات
  كافية (`forward_buffer_secs`)، فيُطلَق الصوت فورًا حتى لو لم يُفكَّ أي
  إطار فيديو بعد — نمط شائع على الأجهزة/المحاكيات ذات فك التشفير البرمجي
  البطيء: الصوت يبدأ ممتازًا، والشاشة تبقى سوداء لثوانٍ (شوهد حتى 7-8
  ثوانٍ) حتى يلحق فك التشفير. الآن لا نخرج من `buffering` ولا نُشغِّل
  الصوت إلا بعد جهوزية إطار مفكوك واحد على الأقل فعليًا
  (`decoded_frame_queue` غير فارغ)، فينطلق الصوت والصورة معًا دائمًا.

## v7.0 — قراءة شبكية بخيوط مستقلة تمامًا (لا تجمّد إطلاقًا) + مهلات زمنية

- **[THREAD-SAFE-NETWORK]** القراءة المستمرة من الشبكة (`av_read_frame`)
  والـ Seek (`av_seek_frame`) كانا يُنفَّذان على الخيط الرئيسي لكل من
  `fmt_ctx` (الفيديو) و`ext_fmt_ctx` (الصوت الخارجي الشبكي). إن تجمّد
  الاتصال (Socket مفتوح لكن بلا بيانات ولا خطأ) كان هذا يُجمّد محرك Godot
  بالكامل لمدة غير محدودة. أصبح لكل منهما خيط خلفي مستقل تمامًا
  (`_network_read_worker` / `_ext_network_read_worker`) يملك سياقه
  حصريًا؛ الخيط الرئيسي لا يستدعي أي دالة `av_*` عليهما إطلاقًا — فقط
  يقرأ من طوابير محمية بـ `std::mutex` يملؤها هذان الخيطان. عمليات الـ
  Seek على مصدر شبكي أصبحت طلبات ذرية (atomic) غير حاجبة يعالجها كل خيط
  بنفسه.

- **[TIMEOUT]** أُضيف `timeout`/`rw_timeout` (15 ثانية) لفتح الفيديو
  الشبكي (كان موجودًا مسبقًا فقط لفتح الصوت الخارجي)، فيحدّ أقصى مدة
  انتظار لأي استدعاء قراءة منفرد حتى داخل الخيط الخلفي نفسه — يضمن ألا
  يُحتجَز أي خيط إلى الأبد على اتصال ميت تمامًا بلا خطأ صريح.

- **[محدودية معروفة — TODO مستقبلًا]** عند إغلاق الخيوط الخلفية في
  `_cleanup()` (تبديل فيديو جديد أو إغلاق التطبيق)، ننتظرها بـ `join()`
  قبل تحرير `fmt_ctx`/`ext_fmt_ctx`. هذا الانتظار محدود بـ 15s كحد أقصى
  (بفضل TIMEOUT أعلاه)، لكنه قد يُحسّ به فقط إن حدث التبديل/الإغلاق
  بالضبط أثناء انقطاع شبكي كامل — وليس أثناء التشغيل العادي إطلاقًا. الحل
  الجذري لإزالته نهائيًا هو نمط "Detach + تنظيف ذاتي": يُمرَّر `fmt_ctx`
  كمتغيّر محلي لكل خيط (بدل الاعتماد على عضو الكائن)، ويُغلقه الخيط نفسه
  ويخرج بمفرده دون أن ينتظره الخيط الرئيسي، مع رقم جيل (generation
  counter) يُميّز حزم الجلسة القديمة عن الجديدة كي لا تتسرب حزم من خيط
  يتيم متأخر إلى طوابير جلسة تحميل جديدة بدأت بالفعل. أُجِّل هذا لتفادي
  التعقيد الإضافي ومخاطر تلوّث الطوابير بين الجلسات دون داعٍ ملحّ حاليًا.

## v6.5 — كشف وتصحيح انجراف التزامن (صوت خارجي محلي)

- **[AV-SYNC]** مراقبة دورية (كل ثانية) لموضع الصوت الخارجي المحلي
  الحقيقي (`AudioStreamPlayer::get_playback_position()`) مقابل موضع
  الفيديو، مع تصحيح صامت للفروق الصغيرة وإصدار إشارة `av_sync_issue`
  للفروق الكبيرة الملحوظة. غير مطلوب للصوت المدمج/الشبكي لأن موضع
  الفيديو هناك مُشتَق حرفيًا من ساعة الصوت (لا انجراف ممكن أصلاً).

## v6.4 — تخزين مؤقت حقيقي قبل الانطلاق

- **[BUFFER-FIX]** شرط الخروج من buffering كان: `forward_buffer_secs >=
  INITIAL_PLAY || !decoded_frame_queue.empty()`. بما أن أول إطار يُفكّ
  خلال أول Tick تقريبًا، كان الشرط الثاني (OR) يُخرِج من التخزين فورًا
  بغض النظر عن حجم البيانات الفعلي المُخزَّن — فلا ينتظر أبدًا 5 ثوانٍ
  كاملة على شبكة ضعيفة، مما يسبب انطلاقًا فوريًا بجزء من الثانية ثم
  نضوبًا سريعًا وتكرار التقطّع. أصبح الشرط يعتمد فقط على
  `forward_buffer_secs` (أو نفاد المصدر كليًا عبر EOF)، ويُستخدم هدف
  أقصر (`REBUFFER_TARGET=2s`) لإعادة التخزين بعد أول انطلاق ناجح
  (`first_buffer_done`) بدل إعادة انتظار 5s في كل مرة.

- **[STATUS-FIX]** إشارة `buffering_status` (المغذّية لشريط التحميل في
  الواجهة) كانت لا تُطلَق إطلاقًا أثناء `buffering=true` بسبب `return`
  مبكر في `_process()` قبل الوصول لكود الإشارة. أصبحت تُطلَق دائمًا
  (أثناء التخزين وأثناء التشغيل العادي) فيعكس الشريط التقدّم الحقيقي.

## v6.3 — فتح الفيديو الشبكي بشكل غير متزامن (Async)

- **[ASYNC-VIDEO]** `avformat_open_input()`/`avformat_find_stream_info()`
  لروابط الشبكة كانا يُنفَّذان بشكل متزامن على الخيط الرئيسي في
  `load_video()`، مما يُجمّد محرك Godot بالكامل لثوانٍ (شاشة سوداء + توقف
  الواجهة). أصبحا يُنفَّذان في `std::thread` منفصل تمامًا كما هو الحال
  مع الصوت الشبكي، وتُسلَّم النتيجة إلى الخيط الرئيسي عبر أعلام atomic
  تُفحَص في `_process()`.

- **[PROBE-SPEED]** إضافة `probesize=512KB` و`analyzeduration=2s` لفتح
  الفيديو الشبكي (لم تكونا موجودتين سابقًا هنا رغم وجودهما في فتح الصوت
  الخارجي)، لتسريع `avformat_find_stream_info()` على الروابط الشبكية.

## v6.2 — إنهاء حقيقي (EOF) + أولوية صوت ذكية

- **[EOF]** `video_finished` أصبح يعتمد على نفاد الديموكسر فعليًا
  (`AVERROR_EOF`) وتفريغ كل الطوابير، بدل مقارنة `position >= duration`
  التي كانت تفشل بسبب `GPU_LATENCY_OFFSET` السالب.

- **[AUDIO-FALLBACK]** الصوت المدمج مع الفيديو يُشغَّل تلقائيًا وفوريًا
  عند التشغيل (محليًا أو رابط شبكة). عند استدعاء `load_audio()` برابط
  صوت خارجي صالح، يتم التبديل إليه بسلاسة فور جاهزيته (بلا فجوة صمت)
  ويتوقف الصوت الداخلي. إن فشل تحميل الصوت الخارجي أو لم يُطلب إطلاقًا،
  يستمر الصوت الداخلي المدمج تلقائيًا كخيار افتراضي/احتياطي — يتيح هذا
  تبديل اللغة (دبلجة) عبر رابط صوت خارجي دون رفع نسخ فيديو متكررة.

## التحسينات السابقة (v5 → v6)

- **[6] Audio Overrun Protection:** `decoded_audio_queue` /
  `ext_audio_frame_queue` تُمسح بالكامل عند كل Seek أو تغيير مصدر. حجم
  الطابور محدود بـ `MAX_AUDIO_FRAMES` (Ring-Buffer).
- **[7] Audio Auto-Recovery:** إذا تجاوزت الفجوة الزمنية بين إطارين
  صوتيين متتاليين `AUDIO_GAP_RESYNC_S` يُمسح الكودك ويُعاد ضبط الساعة.
  إذا تجاوزت `AUDIO_GAP_SKIP_S` يُجرى `av_seek_frame` للنقطة الحالية دون
  تشغيل ما فاته.
- **[8] Audio Interpolation & Soft Clipping:** `swr_ctx` يستخدم Kaiser
  filter (β=9). بعد التحويل يُطبَّق Soft Clip على كل عينة لإزالة
  الطقطقة الناتجة عن Overflow.
- **[9] Silence Filling:** عند فراغ طابور الإطارات الصوتية يُضخّ 20ms من
  الصمت (zero-padding) للحفاظ على استقرار محرك AudioStreamGenerator.
- **[10] Smart Channel Mapping:** Mono → يُنسخ للقناتين (L/R). 5.1/7.1 →
  Downmix تلقائي إلى Stereo عبر `av_channel_layout` وخوارزمية swr
  المُدمجة.
- **[11] Hard Frame Drop + GPU Latency Offset:** إذا تأخر الفيديو عن
  الصوت > `HARD_DROP_THRESHOLD` (20ms) يُسقَط الإطار فوراً.
  `GPU_LATENCY_OFFSET = -0.02s` لتعويض تأخر العرض (لمزامنة العرض البصري
  فقط، وليس لاكتشاف نهاية التشغيل).
- **[12] إصلاح فتح روابط الصوت HTTP/HTTPS:** خيارات avformat محسّنة:
  `timeout`/`rw_timeout`، إزالة `reconnect_streamed`، إضافة
  `http_persistent=0`، `seekable=0` (وضع Streaming نقي).
